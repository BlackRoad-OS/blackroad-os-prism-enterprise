[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "blackroad-quantum-labs"
version = "0.1.0"
description = "Quantum pedagogy and orchestration toolkits for the BlackRoad platform"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "BlackRoad Labs", email = "labs@example.com" }]
dependencies = [
    "hypothesis>=6.103.4,<7",
    "matplotlib>=3.8,<4",
    "nbconvert>=7.0",
    "nbformat>=5.10",
    "numpy>=1.26,<3",
    "pydantic>=2.6.4,<3",
    "pyyaml>=6.0",
    "regopy>=1.0.0,<2",
    "sympy>=1.12",
    "typer>=0.15.1,<0.22",
]

[project.optional-dependencies]
dev = [
    "mypy",
    "pytest",
    "pytest-cov",
    "ruff",
]
qiskit = ["qiskit", "qiskit-aer", "qiskit-ibm-runtime"]
viz = ["matplotlib"]

[project.urls]
Homepage = "https://example.com/quantum-lab"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.setuptools.packages.find]
where = ["."]
include = ["quantum_lab*", "qlm_lab*"]
