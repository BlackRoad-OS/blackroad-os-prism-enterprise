# <!-- FILE: /ops/backup/restic.env -->
export RESTIC_REPOSITORY="s3:http://minio:9000/lucidia-backups"
export RESTIC_PASSWORD="restic_pass"
# Credentials must be injected at runtime (e.g., via OIDC token exchange or secret manager).
: "${AWS_ACCESS_KEY_ID:?Inject AWS_ACCESS_KEY_ID via your secret manager or an OIDC exchange before sourcing ops/backup/restic.env.}"
: "${AWS_SECRET_ACCESS_KEY:?Inject AWS_SECRET_ACCESS_KEY via your secret manager or an OIDC exchange before sourcing ops/backup/restic.env.}"
export AWS_ACCESS_KEY_ID
export AWS_SECRET_ACCESS_KEY
