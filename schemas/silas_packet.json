{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://blackroad.local/schemas/silas_packet.json",
  "title": "SilasPacket",
  "type": "object",
  "required": [
    "assumptions",
    "logic_steps",
    "code",
    "tests",
    "balanced_ternary"
  ],
  "properties": {
    "assumptions": {
      "type": "array",
      "description": "Foundational assumptions that inform the reasoning flow.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "default": []
    },
    "logic_steps": {
      "type": "array",
      "description": "Ordered list of reasoning steps expressed as natural language strings.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "default": []
    },
    "code": {
      "type": "string",
      "description": "Executable code block that embodies the reasoning strategy.",
      "minLength": 0
    },
    "tests": {
      "type": "string",
      "description": "Test instructions or scripts validating the generated code.",
      "minLength": 0
    },
    "balanced_ternary": {
      "type": "string",
      "description": "Balanced ternary trace describing the emotional coloration of the plan.",
      "pattern": "^[+-0]+$"
    }
  },
  "additionalProperties": false
}
