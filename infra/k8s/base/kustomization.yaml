apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml
  - networkpolicy.yaml
  - podsecurity.yaml
configMapGenerator:
  - name: app-config
    literals:
      - APP_NAME=svc-template
      - ENVIRONMENT=dev
