apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-template
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: svc-template
          image: ghcr.io/yourorg/svc-template-fastapi:dev
          env:
            - name: LOG_LEVEL
              value: DEBUG
