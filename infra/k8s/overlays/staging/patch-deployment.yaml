apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-template
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: svc-template
          image: ghcr.io/yourorg/svc-template-fastapi:staging
          env:
            - name: LOG_LEVEL
              value: INFO
