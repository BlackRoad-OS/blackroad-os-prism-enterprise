timezone: "America/Chicago"
users:
  - name: blackroad
    groups: ["sudo","docker"]
    ssh_authorized_keys: ["ssh-ed25519 AAAA...REPLACE_ME"]
zero_trust:
  trust_threshold: 0.62
tailscale: { enable: true, auth_key: "TS_AUTH_KEY" }
sops_age_pub: "age1xxxxxxxxxxxx"
